/* import { useState } from "react";
import MainPackage from "./MainPackage";

const TestPackage = () => {

    const [bookings, setBookings] = useState([{ id: 0, title: "Titel", comments: "comments", done: "False" }])


    return (
        <div>
        {bookings.map(book => <MainPackage book={book}/>)}
        </div>
    )

}

export default TestPackage; */